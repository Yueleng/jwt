import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "JWT Decoder | Decode and Inspect JSON Web Tokens",
  description:
    "A beautiful, client-side JWT decoder. Paste your JWT token to decode and inspect the header, payload, and signature. Your tokens never leave your browser.",
  keywords: ["JWT", "JSON Web Token", "decoder", "parser", "authentication"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
